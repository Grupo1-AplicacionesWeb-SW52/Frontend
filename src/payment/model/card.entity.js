class Card {
    constructor(
        cardHolderName,
        cardNumber,
        expirationDate,
        cvv
    ) {
        this.cardHolderName = cardHolderName;
        this.cardNumber = cardNumber;
        this.expirationDate = expirationDate;
        this.cvv = cvv;
    }
}
